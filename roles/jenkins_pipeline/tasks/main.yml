---

- name: genera Jenkinsfile node exporter
  ansible.builtin.template:
    src: node_exporter.j2 # qui variabile per poter scegliere quale key??
    dest: "./Jenkinsfile-{{ item.name }}"
  loop: "{{ pipelines }}"

